{
  "name": "e-tax-filing-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "db:migrate": "sequelize-cli db:migrate --config src/config/database.cjs --migrations-path src/migrations --seeders-path src/seeders --models-path src/models",
    "db:seed": "sequelize-cli db:seed:all --config src/config/database.cjs --migrations-path src/migrations --seeders-path src/seeders --models-path src/models",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.6",
    "csv-parse": "^7.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "express-rate-limit": "7.5.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0",
    "pdfkit": "^0.19.1",
    "pg": "^8.22.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.8"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.5",
    "supertest": "^7.2.2",
    "vitest": "^2.1.8"
  }
}
